import { Sphere } from "three";
import { SphericalBackgroundProps } from "./threeJsComponents.types";

export const SphericalBackground = ({ name }: SphericalBackgroundProps) => {
  const sphereGeometry = new Sphere();
};
