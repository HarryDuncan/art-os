import { UniformConfig } from "../../../buildShader.types";

export const OPACITY_FUNCTIONS = [];

export const OPACITY_UNIFORMS = {
  defaultUniforms: ["uOpacity"],
  customUniforms: [],
} as UniformConfig;

export const OPACITY_VARYINGS = [];
