import { UniformConfig } from "../../../types";

export const OPACITY_FUNCTIONS = [];

export const OPACITY_UNIFORMS = {
  defaultUniforms: ["uOpacity"],
  customUniforms: [],
} as UniformConfig;

export const OPACITY_VARYINGS = [];

export const OPACITY_ATTRIBUTES = [];

export const DEFAULT_OPACITY_EFFECT_PARAMS = {
  opacity: 1.0,
  asUniform: true,
};
