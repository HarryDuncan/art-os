version: "3"
services:
  proxy:
    build:
      context: ./envoy
      dockerfile: Dockerfile
    ports:
      - "8000:8000"
  server:
    build:
      context: ./interaction_node
      dockerfile: Dockerfile

    ports:
      - "1995:1995"

    depends_on:
      - proxy
